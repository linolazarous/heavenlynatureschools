# Core FastAPI backend
fastapi==0.128.0
uvicorn[standard]==0.40.0

# MongoDB driver (update before deprecation in 2026; consider migrating to PyMongo async API)
motor==3.7.1
pymongo==4.16.0

# Environment variables
python-dotenv>=1.0.1  # still fine, or ==1.0.1

# Authentication / Security
python-jose>=3.5.0    # bumped from 3.3.0
pyjwt>=2.11.0         # from log/install
bcrypt==4.1.3         # current
passlib>=1.7.4
cryptography==46.0.4  # latest stable as of early 2026
email-validator>=2.3.0

# AWS SDK (if using S3 or other AWS services)
boto3>=1.42.41        # from your install log; latest ~1.42.x or higher
requests-oauthlib>=2.0.0
requests>=2.32.5

# Date/Time support
tzdata>=2025.3        # bumped to match log

# Optional: Data processing if needed (pandas 3.0+ is quite new)
pandas>=3.0.0
numpy>=2.4.2          # from log

# Multipart file upload support
python-multipart>=0.0.22  # from log

python-jose[cryptography]>=3.5.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.1.3
